# Lanternfall 설계

## 핵심 가치

**예쁨과 도파민.** 모든 결정은 이 둘로 판단한다. 규칙이 영리해도 화면이 덜 예뻐지면 뺀다. 퍼즐이 깊어져도 터지는 순간이 작아지면 뺀다. 이 게임은 "풀었다"가 아니라 "터졌다"로 기억되어야 한다.

한 판은 20초에서 2분. 세로 화면, 한 손가락, 소리 켜고 하는 게임. 되돌리기 없음.

## 규칙

1. 등불을 톡 치면 터진다. 아래 매듭 수(1~3)만큼 상하좌우로 불꽃이 날아간다.
2. 불꽃은 판 끝, 바위, 또는 **처음 만나는 등불**에서 멈춘다. 만난 등불도 터진다. 이렇게 연쇄가 이어진다.
3. 정해진 탭 수 안에 모든 등불을 터뜨리면 성공.

실패는 둘. 탭을 다 썼는데 등불이 남음. 잠든 등불에 불꽃이 닿음.

**꾹 누르기 미리보기**: 누르고 있으면 그 등불의 불꽃 경로가 금색 점선으로 보이고 맞을 등불이 밝아진다. 0.38초 넘게 누르면 떼도 터지지 않는다. 1차 사격만 보여주고 연쇄는 보여주지 않는다. 연쇄를 머리로 잇는 것이 퍼즐이다.

## 등불과 칸 종류

판 데이터의 등불은 `[행, 열, 사거리, 겹, 종류]`. 종류 값은 코드와 `sim.py`가 공유한다.

| 이름 | 데이터 | 규칙 | 표시 | 첫 등장 |
| --- | --- | --- | --- | --- |
| 등불 | 종류 0 | 기본 | 매듭 1~3개 | 1 |
| 잠든 등불 | `sleep` 목록 | 불꽃이 닿으면 실패. 누를 수 없음(누르면 뒤척임) | 차가운 먹청, 감은 눈, z z | 3 |
| 바위 | `rocks` 목록 | 불꽃을 막음 | 먹색 돌 | 5 |
| 겉종이 등불 | 겹 2 | 두 번 맞아야 터짐. 직접 쳐도 한 겹 | 바깥 테두리 한 겹 | 8 |
| 금띠 등불 (가로) | 종류 1 | 좌우로만 | 가로 금띠 | 11 |
| 금띠 등불 (세로) | 종류 2 | 위아래로만 | 세로 금띠 | 11 |
| 심지 등불 | 종류 3 | 맞으면 한 박자 뒤에 터짐. 기다리는 동안 불꽃을 막음 | 위에 심지, 불붙으면 반짝임 | 14 |
| 큰 잠꾸러기 | `sleep`에 `[행, 열, 2]` | 불꽃이 한 번 닿으면 뒤척이기만 하고(보통 잠든 등불이 됨) 두 번째에 깸. 불꽃은 거기서 멈춤 | 더 크고 어두운 잠든 등불, Z Z | 21 |
| 거울 | `mirrors` 목록 `[행, 열, 종류]` (0 `/`, 1 `\`) | 불꽃이 직각으로 꺾여 계속 감. 거울 칸도 사거리 한 칸으로 셈. `/`: (dr,dc)→(-dc,-dr), `\`: (dr,dc)→(dc,dr) | 청동 막대, 맞으면 번쩍 | 25 |
| 쌍둥이 등불 | 종류 4, 여섯째 값이 짝 번호 | 하나가 터지기로 정해지면 짝도 같은 파도에 예약됨(겉종이 무시). 직접 쳐도 같음. 파도 안 순서는 [맞은 것, 짝] | 종이 가운데 오방색 둥근 바탕에 같은 한글 한 글자(달·별·꽃·강) | 31 |
| 별꽃 등불 | 종류 5 | 대각선 포함 여덟 방향. 방향 순서는 아래·위·오른·왼 다음에 (1,1)(1,-1)(-1,1)(-1,-1) | 금빛 여덟 살 후광이 천천히 돎 | 35 |

한 판에 특수 재료는 최대 두 종류. 바위는 지형이라 세지 않는다.

## 연쇄 계산 (정확한 정의)

게임 코드(`game.js`의 `chain`)와 `sim.py`의 `run`은 이 정의를 똑같이 따른다. 한 곳을 고치면 반드시 다른 곳도 고치고 `e2e.py`를 돌린다.

- 탭한 등불의 겹을 하나 줄인다. 남으면 그 탭은 끝(겉종이만 벗겨짐).
- 파도 0에 탭한 등불을 넣는다. 파도 w를 처리할 때, 그 파도의 등불을 **목록 순서대로 하나씩** 판에서 지운 뒤 불꽃을 쏜다. 같은 파도의 뒤쪽 등불은 아직 판에 있어서 앞 등불의 불꽃을 막는다.
- 방향 순서는 아래, 위, 오른쪽, 왼쪽. 금띠 등불은 이 순서에서 자기 방향만 남긴다.
- 불꽃이 등불을 만나면 그 등불의 겹을 하나 줄이고 멈춘다. 겹이 0 이하가 되고 아직 예약되지 않았으면 예약한다. 보통 등불은 파도 w+1, 심지 등불은 w+2.
- 예약된 등불은 자기 파도가 올 때까지 판에 남아 불꽃을 막는다.
- 잠든 등불을 만나면 hp를 하나 줄이고 멈춘다. hp가 0이 되면 실패(큰 잠꾸러기는 hp 2로 시작해 첫 번은 뒤척임).
- 거울 칸은 방향을 바꾸고 지나간다(사거리 한 칸 소모). 등불 여덟 방향은 별꽃만.
- 쌍둥이가 예약되면 짝도 같은 파도 목록 끝에 예약된다(짝의 겹은 0으로).
- 예약된 파도가 남아 있는 한 계속한다. 빈 파도(심지를 기다리는 중)는 한 박자 쉬고 넘어간다.

## 50단계 구성

다섯 장, 장마다 열 판. 장이 바뀌면 등불 색이 바뀌고 하늘에 새 별자리가 생긴다.

| 장 | 이름 | 등불 색 | 별자리 | 단계 | 판 | 새 요소 | 상태 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 첫 밤 | 주홍 | 초롱자리 | 1~10 | 5×5→7×7 | 잠든 등불, 바위, 두 번(4), 겉종이, 세 번(9) | 완성 |
| 2 | 바람 부는 밤 | 청록 | 연자리 | 11~20 | 6×6→7×7 | 금띠, 심지, 세 번 | 완성 |
| 3 | 깊은 밤 | 자주 | 풍경자리 | 21~30 | 7×7→8×8(27) | 큰 잠꾸러기, 거울 | 완성 |
| 4 | 축제의 밤 | 금빛 | 부채자리 | 31~40 | 7×7·8×8 | 쌍둥이, 별꽃 | 완성 |
| 5 | 새벽 | 연분홍 | 학자리 | 41~50 | 8×8 | 없음. 총정리, 50은 18등불 세 번 | 완성 |

각 장의 첫 판은 새 하늘을 보여주는 판이다. 한 번에 7개 이상, 3파도 이상 터지게 만든다.

## 판 데이터 형식

```json
{ "n": 7, "k": 2,
  "lanterns": [[행, 열, 사거리, 겹, 종류], ...],
  "rocks": [[행, 열], ...],
  "sleep": [[행, 열, (hp)], ...],
  "mirrors": [[행, 열, 종류], ...],
  "sol": [[[행, 열], [행, 열]], ...] }
```
`lanterns` 항목은 `[행, 열, 사거리, 겹, 종류, (짝번호)]`.

3~5장 판(21~50)은 `tools/gen3.py`로 만들었다. 각 장 첫 판은 한 번에 10개 이상 · 5파도 이상. 44부터 8×8. 38·45처럼 겉종이만 벗기는 탭이 한 번 들어간 판이 있다(허용 범위). 요소가 장식으로만 남는 판(거울에 불꽃이 안 닿는 등)은 생성기가 버린다.

`sol`은 되는 모든 순서. 게임에 넣을 때는 `sol`을 뺀다.

## 판 생성 원칙

1. **답이 하나.** 모든 탭 순서, 같은 등불 두 번 누르기까지 전부 시뮬레이션해서 성공하는 탭 묶음이 딱 하나여야 한다. 순서만 다른 것은 같은 답으로 친다.
2. **적은 탭으로 안 풀림.** 두 번 판은 한 번으로, 세 번 판은 두 번으로 절대 안 풀려야 한다.
3. **모든 탭이 크게.** 각 탭이 3개 이상 연쇄, 최대 연쇄 4개 이상. 겉종이만 벗기는 탭은 한 번까지 허용.
4. **잠든 등불은 답을 좁히는 장치.** 잠든 등불 없이 답이 여럿인 판을 뽑고, 정답의 불꽃은 지나가지 않고 오답들의 불꽃은 반드시 지나가는 칸에 잠든 등불을 놓는다.
5. **세 번 판은 합성한다.** 무작위로 뿌리면 답이 없거나(88%) 여럿(11%)이라 거의 안 나온다. 한 번에 풀리는 무리 셋을 따로 만들어 붙인 뒤 전체를 다시 검사한다(`compose.py`).
6. **아직 없는 검사**: 사람이 추론으로 풀 수 있는가. 1차 사격 정보만으로 연쇄를 N단계까지 상상해서 답이 정해지는지. 막히는 판이 나오면 만든다.

## 난이도 (시험판 14)

「모든 단계가 쉽다, 50단계도 금방」 → 4번부터 전부 다시 뽑았다(`tools/gen4.js`, 조건은 `tools/hard_specs.js`). 1~3번만 한 번 판 연습으로 남기고, **4번부터 두 번, 9번부터 세 번 판**이 섞인다. 6번부터 7×7, 27번부터 8×8. 등불은 10→18개.

gen3의 원칙(답 하나 · 적은 탭으로 안 풀림 · 탭마다 크게 · 잠든 등불은 답을 좁힘)에 세 가지를 더했다.

1. **아깝게 안 되는 묶음(near)**: 정답이 아닌데 등불을 1~2개만 남기는 탭 묶음이 3개(뒤쪽 판은 4개) 이상. 찍으면 「거의 됐는데」에서 자꾸 막히고, 따져야 풀린다.
2. **그럴듯한 첫 탭(spread)**: 한 번 탭으로 등불 절반 이상이 터지는 등불이 3개 이상. 어디서 시작할지부터 고민하게.
3. **같은 등불 두 번은 답이 아니다**: 겉종이를 벗기고 다시 누르는 답은 버린다(따질 게 없다).

세 번 판은 무작위로는 안 나와서(원칙 5) 세 구역(가로띠·세로띠·대각)에 한 번짜리 무리를 만들어 붙이는 합성으로 뽑는다. 합성 판에는 잠든 등불을 못 놓는다(무리끼리 부딪혀 답이 여럿이 되는 판이 대부분이라 놓을 자리가 안 나옴) — 그 대신 near 조건이 어렵게 만든다.

생성기는 파이썬(`sim.py`)과 규칙이 같은 노드 판 `gen4.js`다. 옛 50판의 정답과 무작위 탭 350건으로 두 엔진이 같은 답을 내는지 확인했다. 2코어에서 47판이 20분. 옛 판은 `data/levels_v13.json`.

| 판 | 크기 | 탭 | 등불 | 잠든 | 거울 |
| --- | --- | --- | --- | --- | --- |
| 1 | 5×5 | 1 | 7 | 0 | 0 |
| 2 | 5×5 | 1 | 8 | 0 | 0 |
| 3 | 5×5 | 1 | 8 | 1 | 0 |
| 4 | 6×6 | 2 | 10 | 1 | 0 |
| 5 | 6×6 | 2 | 11 | 1 | 0 |
| 6 | 7×7 | 2 | 12 | 1 | 0 |
| 7 | 7×7 | 2 | 12 | 1 | 0 |
| 8 | 7×7 | 2 | 13 | 1 | 0 |
| 9 | 7×7 | 3 | 12 | 0 | 0 |
| 10 | 7×7 | 3 | 14 | 0 | 0 |
| 11 | 7×7 | 2 | 12 | 1 | 0 |
| 12 | 7×7 | 2 | 12 | 1 | 0 |
| 13 | 7×7 | 2 | 13 | 1 | 0 |
| 14 | 7×7 | 2 | 12 | 1 | 0 |
| 15 | 7×7 | 2 | 13 | 1 | 0 |
| 16 | 7×7 | 3 | 14 | 0 | 0 |
| 17 | 7×7 | 2 | 13 | 1 | 0 |
| 18 | 7×7 | 3 | 14 | 0 | 0 |
| 19 | 7×7 | 2 | 14 | 1 | 0 |
| 20 | 7×7 | 3 | 14 | 0 | 0 |
| 21 | 7×7 | 2 | 12 | 1 | 0 |
| 22 | 7×7 | 2 | 13 | 2 | 0 |
| 23 | 7×7 | 2 | 13 | 2 | 0 |
| 24 | 7×7 | 2 | 13 | 2 | 0 |
| 25 | 7×7 | 2 | 12 | 1 | 1 |
| 26 | 7×7 | 2 | 13 | 1 | 2 |
| 27 | 8×8 | 2 | 14 | 2 | 2 |
| 28 | 8×8 | 2 | 14 | 1 | 2 |
| 29 | 8×8 | 3 | 15 | 0 | 2 |
| 30 | 8×8 | 3 | 14 | 1 | 2 |
| 31 | 7×7 | 2 | 12 | 1 | 0 |
| 32 | 7×7 | 2 | 13 | 1 | 0 |
| 33 | 8×8 | 2 | 14 | 1 | 0 |
| 34 | 8×8 | 2 | 14 | 1 | 1 |
| 35 | 7×7 | 2 | 12 | 1 | 0 |
| 36 | 8×8 | 2 | 13 | 1 | 1 |
| 37 | 8×8 | 2 | 14 | 1 | 0 |
| 38 | 8×8 | 2 | 14 | 1 | 0 |
| 39 | 8×8 | 3 | 15 | 0 | 0 |
| 40 | 8×8 | 3 | 16 | 1 | 0 |
| 41 | 8×8 | 2 | 14 | 1 | 2 |
| 42 | 8×8 | 2 | 14 | 2 | 0 |
| 43 | 8×8 | 2 | 15 | 2 | 0 |
| 44 | 8×8 | 2 | 15 | 1 | 3 |
| 45 | 8×8 | 2 | 15 | 1 | 0 |
| 46 | 8×8 | 3 | 16 | 0 | 2 |
| 47 | 8×8 | 3 | 16 | 0 | 0 |
| 48 | 8×8 | 3 | 14 | 0 | 0 |
| 49 | 8×8 | 3 | 16 | 1 | 2 |
| 50 | 8×8 | 3 | 18 | 0 | 2 |

## 화면

- 위에서부터: 별자리 하늘, 제목 줄(단계, Lanternfall, 남은 탭, 다시, ?), 판, 강물.
- 강물에 판이 비친다(`-webkit-box-reflect`, 파이어폭스는 비침 없이 물결만).
- 판이 시작되면 등불이 아래에서 떠올라 제자리에 걸린다.
- 먹빛 하늘. 빛나는 것은 등불과 불꽃뿐.

## 연출과 도파민

**불티(시험판 14.1)**: 등불이 터지면 종잇조각과 함께 제미나이 불티 그림 3~5개(`sparks()`)가 위쪽 반원으로 튀어 포물선으로 떨어진다. 꼬리는 진행 방향 반대. 가운데 빛 번짐은 둥근 불티 그림을 가림막으로 써 장 색으로 칠한다(`.glow`). 둘 다 `mix-blend-mode: screen`.

**색종이와 강물(시험판 14.2)**: 판을 깨면 제미나이 오방색 한지 조각 12가지가 결과 카드 위로 64개 내려온다(`rain()`, 세 축 회전 + 좌우 흔들림). 강물은 제미나이 물결 그림이 `.water` 배경으로 90초에 한 폭씩 흐른다. 강물은 판 폭이 아니라 **화면 전체 폭**(100vw)이다 — 데스크톱에서 판 폭만큼만 보이던 것을 고침(14.3). 코드로 그리던 사각 조각·색띠 강물은 없어졌다. **등불이 터질 때 튀는 조각(`.scrap`)과 인트로 제목 등불의 조각도 같은 색종이 그림**을 쓴다(15.1) — 사각 색 조각은 어디에도 남지 않았다.


| 순간 | 연출 |
| --- | --- |
| 터짐 | 하얗게 번쩍 → 종이 조각 8~14개 팔랑이며 낙하 → 잔광 1.2초 |
| 불꽃 | 금박 조각이 회전하며 칸당 0.11초 |
| 연쇄 5개 이상 | 판 가운데 ×N |
| 한 파도 3개 이상 | 판이 흔들리고 하늘이 따뜻하게 밝아짐 |
| 누적 12개 이상 | 금가루가 위로 날림 |
| 마지막 등불 | 0.22초 숨 참고, 크게 터지며 해결 화음, 금가루 |
| 판 성공 | 불씨가 하늘로 날아가 별 하나를 켬, 종이 조각 비 |
| 장 완성 | 별자리 이름이 뜨고 반짝임 |
| 잠든 등불 깨움 | 눈을 번쩍 뜨고 붉어짐, 하품 소리 |

## 설명 카드

첫 실행에 규칙 셋 카드. 새 요소가 처음 나오는 판 직전에 그 요소 카드 한 장. 본 카드는 저장되어 다시 안 뜬다. **?** 버튼과 실패 화면에서 본 요소를 모은 규칙집을 연다. 새 요소를 추가하면 `game.js`의 `CARDS`, `ORDER`, `featuresOf`에 같이 넣는다.

## 사운드 (시험판 16)

**축하음(16.4)**: 판을 깨면 `toneClear()`가 **여섯 가지**(오름 아르페지오 · 내려갔다 치솟기 · 세 화음 굴림 · 두 옥타브 뛰기 · 종소리 물결 · 물음-대답) 중 바로 앞과 다른 것을 골라 친다. 모두 장 으뜸음 5음계, 첫 박은 배경음 반박자에 맞춤. 장을 다 깨면(`big`) 조금 크게 치고 1초 뒤 여섯 음 화음을 한 번 더 겹친다.

**햅틱(16.4)**: `HAPTIC` 표(16.5, 「더 강하게」로 두 배쯤) — 탭 20ms · 겉종이 벗기기 30 · 파도마다 30+등불수×12(최대 90) · 마지막 등불 [90,40,160] · 판 깸 [70,50,70,50,200] · 장 완성 [80,50,80,50,120,50,300] · 잠든 등불 깨움 [150,70,150] · 실패 [120,60,120] · 단추 15 · 별 켜짐 45. `navigator.vibrate`라 안드로이드 크롬에서만 떨리고 아이폰 사파리는 조용히 넘어간다(iOS는 웹에 진동 API가 없다).


**지금(16)**: 효과음은 **음정 있는 「팡」**(`FX_MODE='tone'`). 「등불 터질 때 국악기 소리가 마음에 걸린다」 → 배경음이 장구·북 리듬 위주가 되었으니 터지는 소리는 타악기가 아니라 가락 자리를 채운다. 작은 등불 = 칼림바(한 옥타브 위), 중간 = 마림바, 큰 등불 = 마림바 한 옥타브 아래 + 옥타브 위 살짝. 파도마다 5음계로 한 칸씩 오르고(`PENT`), 장 으뜸음(`KEYS`)을 따른다. 밑에 한지 「툭」 잡음(`paperTok`)을 얇게 깐다. 마지막 등불 = 도·미·솔 마림바 화음 + 칼림바 높은 도. 판 깨기 = 마림바 아르페지오가 배경음 반박자 격자를 타고 올라가 칼림바로 맺음(`samulClear`의 tone 분기). 후보 A 마림바·B 칼림바·C 유리종을 `tools/sfx_tone.py`로 미리듣기 만들어 들려 주고 A+B로 정했다. 마림바·칼림바·종은 배음 구조가 단순해 합성으로도 진짜처럼 나온다(사물놀이 합성이 가짜 같았던 것과 다름). 국악기 녹음과 사물 장단 코드는 `FX_MODE='samul'`로 되돌릴 수 있게 남겨 둠.

### 예전 (시험판 13~15)

**지금(13)**: 효과음은 다시 **진짜 국악기 녹음**(`FX_MODE='samul'`, `sfx/gugak/`). 작은 등불 꽹과리, 중간 등불 장구 덩·덕, 큰 등불 북+궁편, 마지막 등불 북+꽹과리+징, 판을 깨면 사물놀이 한 마디(`samulClear`) — **휘모리·자진모리·굿거리·세마치·별달거리·엇모리 여섯 장단**(`JANGDAN`) 중 바로 앞과 다른 것을 골라 매번 다르게 치고, 첫 박은 배경음 반박자에 맞춘다(14). 사물 버스(`SAM`) .45.

**박자 맞춤(리듬 요소)**: 연쇄의 파도(wave) 하나하나가 배경음의 **반박자 격자**에 맞춰 터진다. `tools/beats.py`가 `music/ch*.mp3`의 bpm·첫 박 위치를 재서 `music/beats.json`에 쓰고, `game.js` `nextTickMs(min)`이 「지금 곡 시각 + 최소 대기」 이후 첫 반박자까지의 ms를 돌려준다. `chain()`의 파도 사이 대기가 이 값을 쓴다(마지막 등불의 큰 대기는 제외). 그래서 효과음이 저절로 배경음의 반주가 된다. 곡이 없거나 멈춰 있으면 원래 대기 그대로. 배경음을 바꾸면 `python3 tools/beats.py`를 다시 돌려야 한다. **구간별 박자(15)**: 수노 곡은 템포가 1~2% 흔들려(ch4는 127.7→131) 한 값으로는 뒤로 갈수록 반박자까지 어긋난다. 그래서 40초 창을 30초씩 밀며 구간마다 bpm·위상을 재어 `segs`로 두고, `beatSeg()`가 지금 곡 시각의 구간을 골라 쓴다.

배경음은 이 효과음과 어울리는 **신나는 타악 중심 곡**(수노 4차, 가락 악기 거의 없음 — 중국풍·트로트를 피하려고; `assets.md` 음악 4차)으로 바꿨다(15). 연쇄 동안 배경음을 낮춘다(`duck`). 16.1: 배경음 버스 .5→.72(`MUS_VOL`), 연쇄 중 .14→.3(`DUCK_VOL`) — 「bgm이 조금 더 컸으면」. 16.3: 효과음 버스 `SAM` .45→.7 — 「터지는 효과음이 좀 더 컸으면」.

### 예전 (시험판 12)

배경음이 사물놀이를 맡고 효과음은 한지 「톡」+가야금 한 음(`FX_MODE='pluck'`)이었다. 「그냥 효과음은 국악기로」 → 13에서 되돌림. pluck 코드는 남아 있다.

### 예전 기록 (시험판 10~11)

소리는 **기본 켜짐**, 첫 터치에 시작. 스피커 단추로 끄고 켠다.

- **효과음 = 사물놀이. 시험판 11부터 진짜 국악기 녹음(`sfx/gugak`, assets.md), 아래 합성은 녹음이 없을 때의 대체**. 녹음 파일(폭죽 두 번 시험)은 소리가 뭉툭하고 펑의 정점이 늦어 연쇄와 싱크가 안 맞았다 → 합성으로 결정. 합성은 시작점이 0초라 화면과 딱 붙는다.
  - 작은 등불(1칸) **꽹과리 「갱」**: 1180Hz 비조화 배음 여섯(1, 1.47, 2.09, 2.76, 3.52, 4.3) + 채 끝 딱. 파도 세 번에 한 번은 막음(짧게)
  - 보통 등불(2칸) **장구 「덩」·「덕」 번갈아**: 채편(420→290Hz 삼각파 + 1.9kHz 잡음), 궁편(150→92Hz)
  - 큰 등불(3칸) **북 「쿵」** + 장구 궁편
  - 마지막 등불 **북 + 꽹과리 + 징 「지잉~」**(196Hz × 장 으뜸음, 맥놀이하는 배음 쌍, 3.6초)
  - 판을 깨면 **사물놀이 한 마디**(`samulClear`): 꽹과리 열두 번 몰아치고 장구·북이 받치다 징으로 맺음. 1.9초
  - 모든 팡 밑에 한지 「파삭」을 얇게 깐다. 연쇄가 이어질수록 조금씩 높고 세게(최대 +14%)
  - 세기: 사물놀이 버스 `SAM` .55. 한 번 .36~.47, 연쇄 겹침 .5대, 마지막 .76, 한 마디 .82(압축기 앞 최고치)
- **배경음**: `music/chN.mp3`(수노) 반복, 없으면 합성 가야금 가락. 곡 끝에서 2.5초 페이드인으로 다시 시작
- **밤소리**: 1장 풀벌레·소쩍새·물결 / 2장 + 강바람 / 3장 풍경 / 4장 먼 북 / 5장 새벽 새. 사인 삑 소리는 모두 잡음·떨림 섞인 소리로 바꿨다
- 녹음 효과음을 다시 쓰려면 `game.js`의 `SFX_DIR`에 `sfx/hq` 등 폴더를 넣는다(지금 '')
- 다음 후보: 4장 「축제의 밤」 배경음을 사물놀이풍 수노 곡으로. 연쇄 파도 간격을 장단 박자에 맞춰 양자화

## 가볍게 만들기 (시험판 9)

「소리가 딜레이되고 프로그램이 무겁다」는 말에 찾은 것들. 앞으로도 지킬 것.

- **filter 금지(움직이는 것에는)**: 등불 빛 번짐은 `drop-shadow` 필터였고, 등불이 흔들리니 매 프레임 다시 계산했다. 지금은 `.lan::before`에 깔린 방사형 그림 한 장. 바위·문양·별꽃 후광의 필터도 뺐다. 필터는 잠깐 켜지는 것(맞을 때 밝아짐, 터짐)에만.
- **소리 켜는 함수는 장면을 한 번만 만든다**: `audio()`가 터치마다 `setScene()`을 불러 물결·바람 잡음 발생기와 흔들림 장치를 새로 만들고 예전 것을 4초 동안 꺼서, 빠르게 탭하면 장치가 쌓였다. 지금은 `bed`가 없을 때만, 그리고 같은 장이면 건너뛴다.
- **잔향 1.4초·wet .32, 압축기 threshold -12·ratio 3**: 2.8초 잔향과 기본 압축기(-24dB·12:1)가 팡을 뭉갰다.
- 효과음 파일은 읽어 들일 때 실제 시작점(최고치의 8%)을 찾아 그 지점부터 튼다. mp3 앞의 빈 구간이 탭보다 소리를 늦게 냈다.
- 검사 방법: `python3 -m http.server`로 열고(파일 소리는 http에서만), 탭 뒤 첫 소리까지 ms와 최고치를 analyser로 잰다. 터치 열 번에 `createBufferSource` 호출 수가 팡 수 + 1이어야 한다.
- 남은 무거운 것: `-webkit-box-reflect`(판 전체 비침). 사파리·크롬만 되고 매 프레임 다시 그린다. 그래도 무거우면 이것을 끄는 설정을 넣는다.

## 화면 배열 (시험판 13)

- 별자리 이름은 별 그림 아래가 아니라 **오른쪽에 세로 글씨**로(14, 아래에 두면 꼬리 별과 겹쳤다).
- 별자리는 화면 맨 위에 고정(`.sky` fixed). `.play`는 세로 가운데가 아니라 **위에서부터**(`body justify-content: flex-start`) 쌓여, 제목 줄과 메뉴가 별자리 바로 아래에 붙는다(13). 판 아래에 도구줄.
- 위 줄에는 단계 이동 · 제목 · 남은 탭만. **다시 · 규칙 · 소리** 단추는 판 아래 강물 위의 56px 둥근 단추(`.tools`)로 옮겼다. 폰에서 엄지가 닿는 자리.

### 예전 (시험판 8)

위에서부터 별자리 하늘(12vh) → 제목 줄 → 판(92vw·52vh 중 작은 것) → **강가 여백 40px에 원경 실루엣** → 강물(판이 비침). 원경이 판 아래 등불을 가리지 않게 판과 원경 사이를 띄웠고, 비침은 강가 여백만큼 아래에서 시작한다. 제목 줄은 폰에서 글자가 겹치지 않게 제목 크기가 화면 폭을 따른다.

## 인트로 화면 (완료, 시험판 5 · 16.6 강물 그림)

- 16.6: 바닥 물띠(`.thorizon`)도 게임 화면과 같은 제미나이 강물 그림으로. 90초에 한 폭씩 흐르고 위는 먹빛으로 기슭과 이어진다.


- 먹빛 하늘에 반짝이는 별, 강물에서 등불 13개가 천천히 떠오른다. 크기와 속도가 달라 깊이가 생기고, 먼 것은 살짝 흐리다. 연 장의 색(주홍·청록 …)이 섞여 떠오른다.
- **떠오르는 등불도 톡 치면 팡 터진다.** 소리, 종이 조각, 진동까지. 시작 전부터 손맛을 보여주는 장치.
- 가운데 Lantern*fall*, 아래 "등불 터뜨리는 밤". 글자가 흐릿하게 퍼졌다가 모인다.
- 처음이면 "시작하기"만 보이고 → 1단계 → 규칙 카드. 다시 오면 "이어하기 N단계"와 "장 고르기".
- **밤하늘 지도**(장 고르기): 다섯 별자리가 왼쪽 아래 1장부터 지그재그로 올라가고 점선 길로 이어진다. 켜진 별 = 깬 판. 못 연 장은 흐리고, 누르면 흔들린다. 별자리를 누르면 아래에 그 장의 열 판 단추(깬 판 금색, 할 차례 반짝임).
- 게임 화면의 Lantern*fall* 글자를 누르면 인트로로 돌아간다.
- 3~5장 별자리 모양(풍경, 부채, 학)은 제안. `game.js`의 `CH`에 있다. 장 색은 `TINTS`에 미리 넣었다.
- `e2e.py`는 인트로에서 "이어하기"를 누르고 들어간다.
